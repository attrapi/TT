node_modules/
package*.json
# Documentos sueltos que no van al repo
Comparativo-TT-vs-Monday.*
# NUNCA subir credenciales al repo (es publico)
Credenciales-TT.*
*credenciales*
*contrasenas*
# Copias de trabajo de scripts SQL (borradores locales, no van al repo)
*.local.sql

# Cache de Python de las herramientas
__pycache__/
*.pyc
