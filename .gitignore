node_modules/
package*.json
# Documentos sueltos que no van al repo
Comparativo-TT-vs-Monday.*
# NUNCA subir credenciales al repo (es publico)
Credenciales-TT.*
*credenciales*
*contrasenas*
# Copias LOCALES de scripts con correos/datos reales del personal.
# El repo es publico: la version del repo lleva placeholders.
*.local.sql
